import Actions from '../contextActions';
import { IAppState } from './AppState';

type AppAction = any;

const appReducer = (
  state: IAppState,
  action: AppAction
): any => {
  switch (action.type) {
  }
};

export default appReducer;
